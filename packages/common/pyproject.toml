[project]
name = "common"
version = "0.1.0"
description = "Add your description here"
requires-python = "==3.14.*"
dependencies = [
    "pydantic-settings",
    "lancedb",
    "model2vec",
    "huggingface-hub",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
