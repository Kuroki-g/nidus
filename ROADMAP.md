# ROADMAP

## コンセプト

**「AI が使いやすい日本語ローカル検索エンジン」**

差別化の軸：日本語精度 × AI可読性 × 軽量・ローカル

**主なユースケース**: Claude Code・Gemini CLI などの AI エージェントが Bash ツール経由で `nidus search` を直接呼び出す。MCP サーバー（`nidus-mcp`）はサブ機能として提供し、MCP 対応クライアント向けの利便性向上に寄与する。

---

## Milestone 1〜3 ✅完了

AI可読性の改善・対応ファイル形式の拡充・検索精度の向上は完了。詳細は `docs/next_release_feature.md` を参照。

---

## Milestone 4: 運用・配布の改善

**ゴール**: セットアップが簡単で継続的に使いやすい状態

### 完了タスク
- [x] インクリメンタルアップデート（差分のみ再インデックス）— `file_hash` によるハッシュ比較、未変更ファイルをスキップ、`created` 日時を保持

### 完了タスク（追加）
- [x] ウォッチモード（ディレクトリ監視して自動 `add`）— known bugs は `docs/issues/` を参照

