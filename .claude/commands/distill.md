セッション終了処理。2つのサブエージェントを並列起動し、結果を表示する。

## 実行手順

以下の2つを **Agent ツールで並列起動** する。情報源は git とファイルのみ。会話の内容・判断・感想は一切使わない。

---

### サブエージェント A: メモリ整理

`/home/vscode/.claude/projects/-workspace-nidus/memory/MEMORY.md` を更新する。

1. `git log --oneline -20` で最近のコミットを確認
2. 事実のみ記録する:
   - 完了したモジュール名とテスト数
   - 次のアクション
   - 明示的に決定されたこと
3. 削除する:
   - 古くなったエントリ
   - 重複
4. 200行以内に収める

記録しないこと:
- コードへの評価・意見
- 実装中の気づきや「落とし穴」（それはフレッシュレビューで発見させる）
- 一時的な状態

完了したら `done` とだけ返す。

---

### サブエージェント B: コードレビュー

git の情報だけを見て、バイアスなしでコードをレビューする。

1. `git log --oneline -10` で対象コミットを特定
2. `git diff <oldest>^..<newest>` で差分を取得
3. コードを読んでレビューする

レビュー観点:
- 正確性・バグの可能性
- 設計上の問題
- テストの妥当性
- 気になる点

レビュー結果をそのまま返す（要約・評価ではなく、具体的な指摘）。

---

## 出力

サブエージェント B のレビュー結果をそのままユーザーに表示する。
